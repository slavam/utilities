%h1 Список организаций
.content
  %table.data
    %tr
      %th Код
      %th Название
    - for f in @firms
      %tr
        %td= f.code
        %td= link_to f.name.to_utf, :action => 'show', :id => f.code
  = will_paginate @firms